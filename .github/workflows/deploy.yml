---
name: Deploy Action

on:
  workflow_call:
    inputs:
      branch:
        required: true
        type: string
      namespace:
        required: true
        type: string
      user_name:
         required: false
         default: spsh-bot
         type: string
    secrets:
      DEV_KUBE_CONFIG:
        required: true
permissions:
  contents: read

jobs:
  # Create Namespace

  # Search for helm charts

  # Deploy Helmcharts

  deploy-successful:
    needs:
      - deploy
    runs-on: ubuntu-latest
    steps:
      - run: echo "deploy was successful"